# Central migrations

Tables in this directory are central concerns:

- Laravel framework infrastructure
- Stancl `tenants` and `domains`
- LaravelCM plans, features, subscriptions, and usage
- SaaS subscription billing
- Global permissions

They are loaded automatically by `AppServiceProvider`.
